import React from "react";
import { Grid } from '@material-ui/core';

const Home = (props) => {
    return (
      <Grid container spacing={1}>
       {/* landing page */}
      </Grid> 
)
};

export { Home };

